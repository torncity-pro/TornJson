# TornJson
A C# json data definition into the Torn City API using Newtonsoft for deserializing.

This project creates all the concrete classes and files for serializing and deserializing the data returned by the Torn API.
